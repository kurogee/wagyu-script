while #calc(":1: <= :2:" $start $end) {
    if #calc(":1: % 15 == 0" $start) {
        println "FizzBuzz";
    } else {
        if #calc(":1: % 5 == 0" $start) {
            println "Buzz";
        } else {
            if #calc(":1: % 3 == 0" $start) {
                println "Fizz";
            } else {
                println $start;
            };
        };
    };
    $start = add 1;
};